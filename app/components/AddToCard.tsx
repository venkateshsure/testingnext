'use client'

import React from 'react'

const AddToCard = () => {
  return (
    <div>
       <button type="button" onClick={()=> console.log("click")}>Component1</button> 
    </div>
  )
}

export default AddToCard
